import * as Actions from './uiActions'

export const initialState = {
  modalOpen: false
}

const uiReducer = (oldState = initialState, action: Actions.All) => {
  return oldState
}

export default uiReducer
